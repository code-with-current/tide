/** Git tab: the GitPanel directly — working-tree Changes, History, branch
 *  menu, stash, commit bar. (The old Changes/Review wrapper was removed:
 *  Review lives in the commit-details sheet's ✨ actions.) */
export { GitPanel as GitTab } from '@/components/right-panel/git/git-panel';
