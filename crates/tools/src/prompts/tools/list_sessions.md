<!--
name: "list_sessions"
description: "List this workspace's saved chat sessions, newest first."
category: "Agent"
-->
- `list_sessions` — List this workspace's saved chat sessions — id, title, model, message count, and last-updated age, newest first. Use it to find a past conversation whose context would help the current task; the current session is marked. Follow with `read_session` to page through a session's messages.
