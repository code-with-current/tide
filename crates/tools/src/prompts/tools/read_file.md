<!--
name: "read_file"
description: "Read a file from the workspace. Returns its contents as text."
category: "Files"
tideVersion: "1.0.0"
-->
- `read_file` — Read a file from the workspace. Returns its contents as text.