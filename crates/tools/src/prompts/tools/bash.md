<!--
name: "bash"
description: "Run a shell command in the workspace root. Full shell support."
category: "Shell"
tideVersion: "1.0.0"
-->
- `bash` — Run a shell command in the workspace root. Full shell support.