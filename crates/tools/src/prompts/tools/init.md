<!--
name: "init"
description: "Scan the workspace and create a minimal AGENTS.md project config."
category: "Agent"
tideVersion: "1.0.0"
-->
- `init` — Scan the workspace and create a minimal `AGENTS.md` at the project root. The file captures non-obvious project rules, build commands, and gotchas — only what the agent can't infer from the code. Call when the user wants to set up project configuration.
