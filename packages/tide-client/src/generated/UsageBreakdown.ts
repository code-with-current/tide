// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.

/**
 * Per-step token usage breakdown riding alongside context occupancy on
 * [`DriverEvent::UsageUpdated`]. The engine's own `Usage` shape, mirrored
 * here because the protocol crate cannot depend on the engine; each step's
 * values are deltas, so a client sums them for session totals. Absent on
 * payloads from before the field existed and from drivers that never
 * measured one — `None` must render as "unknown", never as zero.
 */
export type UsageBreakdown = { inputTokens: number, outputTokens: number, cacheRead: number, cacheWrite: number, reasoningTokens: number, calls: number,
/**
 * Priced cost of the step; `None` while pricing is unavailable.
 */
costUsd?: number | null,
/**
 * Wall time the model stream took this step, in milliseconds — the
 * full request-to-StepEnd window including time to first token.
 * `None` from drivers that never measured one; must render as
 * "unknown", never as zero.
 */
llmMs?: number | null,
/**
 * Stream start to the step's first streamed delta (text or
 * reasoning), in milliseconds. `None` when the step produced no
 * delta or the driver never measured one.
 */
ttftMs?: number | null,
/**
 * Wall time tool calls spent executing during this step, in
 * milliseconds — the sum of each batch's elapsed wait, so parallel
 * batches count once, not once per call.
 */
toolMs?: number | null, };
