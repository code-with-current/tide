// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { UsageBreakdown } from "./UsageBreakdown";

/**
 * Session-cumulative usage, summed from per-step [`UsageBreakdown`] deltas
 * and persisted on the session so the inspector's Context Window detail
 * survives session switches and relaunches. `last_step` keeps the newest
 * step's composition — the meter segments — not the sum.
 */
export type SessionUsageTotals = { inputTokens: number, outputTokens: number, cacheRead: number, cacheWrite: number, reasoningTokens: number, calls: number,
/**
 * Priced cost accumulated so far; `0.0` while pricing is unavailable.
 */
costUsd: number,
/**
 * Cumulative model-stream wall time, in milliseconds. `0` while no
 * step has reported timing.
 */
llmMs: number,
/**
 * Cumulative tool-execution wall time, in milliseconds.
 */
toolMs: number,
/**
 * Sum and sample count for the time-to-first-token average — kept
 * separately because a step with no streamed delta contributes no
 * sample, and the average must divide samples, not steps.
 */
ttftMsTotal: number, ttftSteps: number, lastStep?: UsageBreakdown | null, };
