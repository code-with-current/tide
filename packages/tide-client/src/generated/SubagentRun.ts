// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { BackgroundWorkStatus } from "./BackgroundWorkStatus";
import type { SubagentBlock } from "./SubagentBlock";

/**
 * One settled dispatched sub-agent run, persisted with its session — the
 * durable record the agents panel rehydrates from after a restart. Keyed
 * by the driver's child id, which also serves as the work-item key so the
 * streaming registry and the persisted run stay matched.
 */
export type SubagentRun = {
/**
 * The durable child identity: the work item's `key.provider_id` and
 * the model-facing `dispatchId` (resumeFrom target).
 */
childId: string,
/**
 * The registry agent that ran (e.g. "code-reviewer").
 */
agentName: string, title: string,
/**
 * The latest task the child was dispatched or resumed with.
 */
task: string | null,
/**
 * The settled block timeline — reasoning, tool runs, narration.
 */
blocks: Array<SubagentBlock>,
/**
 * The final report (the Result card), when the child produced one.
 */
report: string | null, status: BackgroundWorkStatus, durationMs: number | null,
/**
 * The transcript activity of the dispatch call that last touched this
 * run — the Bot button's link back into the transcript.
 */
originActivityId: string | null, };
