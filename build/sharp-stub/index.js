export default function sharp() {
  throw new Error(
    'sharp is stubbed in Tide — image preprocessing is unavailable. ' +
      'Tide only runs text feature-extraction, which never reaches this code path.',
  );
}
