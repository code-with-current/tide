/** Stub for `onnxruntime-web` (aliased via vite.electron.config.ts): @xenova/transformers statically imports it but always picks the Node branch in Electron's utility process, so this empty stub satisfies the import without shipping ~66 MB of dead WASM/JS. */
const stub: Record<string, unknown> = {};

export default stub;
export const env = { wasm: {} };
